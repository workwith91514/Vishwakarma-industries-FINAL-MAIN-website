# Google Analytics 4 Measurement ID (Admin -> Data Streams -> your web stream, in GA4).
# Leave unset locally/in PRs — analytics stays fully disabled until this is set.
# In Netlify: Site configuration -> Environment variables -> add VITE_GA_MEASUREMENT_ID.
VITE_GA_MEASUREMENT_ID=
